/**
 * 취소 권유 댓글의 **고정 문장** — 서버(mutations.suggestCancel)가 쓰고 화면(DetailFlow)이 읽는다.
 *
 * 🔴 두 곳이 같은 상수를 본다. 신청자 상세의 권유 카드는 댓글 본문이 이 문장으로
 *    시작하는지로 판별하므로, 한쪽만 문구를 바꾸면 카드가 **조용히 사라진다**
 *    (실측: 구현 중 서버 문구가 바뀌어 카드 판별이 한 번 깨졌다).
 */

/** 본문 첫 문단 */
export const SUGGEST_CANCEL_LEAD = "담당자가 요청 취소를 권유했습니다.";

/** 본문 끝 안내 문단의 시작. 이 줄부터는 사유가 아니다 (뒤에 붙는 설명은 단계마다 다르다) */
export const SUGGEST_CANCEL_TAIL = "취소 실행은 신청자 본인만";
